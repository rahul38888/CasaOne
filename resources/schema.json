{
	"bsonType": "object",
	"required": ["productid","name"],
	"properties": {
	  "productid": {
		  "bsonType": "int",
		  "description": "product id must be a integer and is required"
		},
	  "name": {
		  "bsonType": "string",
		  "description": "name of the product, must be a string and is required"
		},
	  "color": {
		  "bsonType": "string",
		  "description": "color must be a string if the field exists"
		},
	  "assemblytime": {
		  "bsonType": "int",
		  "description": "assembly time stored in minutes, must be a int if the field exists"
		},
	  "lastassemblies": {
		  "bsonType": "array",
		  "items": {
			  "bsonType": "int",
			  "description": " assembly time stored in minutes, each must be a integer"
		  }
		}
	}
  }